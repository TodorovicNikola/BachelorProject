@model IEnumerable<ConstructIT.DAL.Models.Struka>

@{
    ViewBag.Title = "Struke";
}

<h2>Struke</h2>

<p>
    @Html.ActionLink("Dodaj novu", "Create")
</p>
<hr />
<table class="table table-striped table-hover">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.StrukaNaziv)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.StrukaNaziv)
        </td>
        <td>
            @Html.ActionLink("Izmeni", "Edit", new { id=item.StrukaID }) |
            @Html.ActionLink("Obriši", "Delete", new { id=item.StrukaID })
        </td>
    </tr>
}

</table>
